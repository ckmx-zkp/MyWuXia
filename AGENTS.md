# 江湖长夜：项目总览

本文件是给开发者与协作者的总索引。新增区域、NPC、事件、UI、音频时**先读对应文档**，再回到这里对齐约定。

---

## 0. 文档地图

| 主题 | 文档 |
|---|---|
| 世界地图与政权结构 | `docs/gdd/01-world-map.md` |
| 任务系统与四层结构 | `docs/gdd/02-quest-system.md` |
| 首批 10 棵任务树清单 | `docs/gdd/03-launch-task-trees.md` |
| 大理天龙主线 | `docs/gdd/04-dali-task-tree.md` |
| 音频设计（BGM / SFX / 音量） | `docs/gdd/05-audio-design.md` |
| 地图基础设施（17 设施 / 四母版） | `docs/gdd/06-map-infrastructure.md` |
| 剧本与对话工程框架（五模块 JSON） | `docs/gdd/07-dialogue-framework.md` |
| 江南六城生活支线与循环日常 | `docs/gdd/08-jiangnan-daily-quests.md` |
| 自动交手与存档 | `docs/gdd/09-auto-combat-and-saves.md` |
| 可改变的命运与跨书江湖 | `docs/gdd/10-changing-fates.md` |
| 放置武侠成长框架与 P0/P1/P2 | `docs/gdd/11-idle-rpg-roadmap.md` |
| 各任务树完整剧本 | `docs/gdd/quests/<ID>-<slug>.md` |
| UI 素材规范 | `docs/ui/UI-ASSET-GUIDE.md` |
| 架构优化方案 | `docs/architecture-optimization.md` |

---

## 1. 核心原则

1. 玩家是独立的江湖行动者，**不是任何原著主角的替身**。
2. 原著是初始人物关系与事件起点，**不是必须复演的结局**。玩家保持独立身份，可通过行动改变原著人物的命运、阵营、存亡与重大事件走向；分支必须有条件、代价和后续世界回响。
3. 江湖实力不是唯一权力来源——朝廷、门派、帮会、家族、军队、情报、个人声望共同决定世界状态。
4. 地图采用双层架构：**通用生活区域（17 项基础设施）+ 金庸特色区域**；详见 `docs/gdd/06`。玩家不接原著主线也能完整生活与练级。
5. 练级任务表现为江湖生活：护镖、传信、采药、军营训练、门派切磋、追捕、探索、贸易、救援——**禁止纯杀怪任务**。
6. UI 文案、数值、状态、按钮全部由代码渲染；图片只作背景、卷轴、人物装饰（见 `docs/ui/UI-ASSET-GUIDE.md`）。

---

## 2. 世界结构：13 个一级区域

“五大朝廷 + 三大缓冲区 + 四大江湖边疆 + 海外”：

| 区域 | 性质 | 核心节点 |
|---|---|---|
| 江南东南·大宋 | 宋 | 临安、扬州、苏州、嘉兴、福州、无锡 |
| 荆襄·宋明前线 | 多方争夺 | 襄阳、江陵、武昌、汉水、武当 |
| 中原·天下腹地 | 无绝对霸主 | 洛阳、开封、嵩山、少林 |
| 河朔燕云·大辽 | 辽 | 燕京、大同、幽州、雁门关 |
| 塞北草原 | 辽影响 / 蒙古诸部 | 张家口、王庭、诸营 |
| 辽东·大金 | 金 | 上京、辽阳、沈州、长白南麓 |
| 白山黑水·满清 | 新兴政权 | 盛京、赫图阿拉、宁古塔 |
| 西北·西夏 | 西夏 | 兴庆府、河西走廊、贺兰山 |
| 关中秦地 | 军事缓冲区 | 长安、华山、终南山、潼关 |
| 巴蜀西南 | 明势力圈 | 成都、峨眉、青城、剑门 |
| 云贵·大明 | 明 | 昆明、贵阳、云南府 |
| 大理及西南诸国 | 独立 / 明属国 | 大理、无量山、万劫谷、点苍山 |
| 东海及海外诸岛 | 无统一政权 | 桃花、侠客、神龙、灵蛇、台湾 |

**地图邻接**：

```text
塞北草原 ─ 河朔燕云·辽 ─ 辽东·金 ─ 白山黑水·满清
     │             │             │
西夏 ─ 关中 ─ 中原 ─ 荆襄争夺区 ─ 江南宋境 ─ 东海海外
     │       │          │              │
   河西      华山       襄阳           临安
             │          │
          巴蜀西南 ─ 云贵大明 ─ 大理诸国
```

**政治约束**：

- 中原、关中、荆襄不得划归单一政权；保持地方军镇、豪族、门派与多国势力交错。
- 大辽是高度汉化且保留草原传统的强国，**不是单纯反派**。
- 大金与满清必须分立：金是老牌女真帝国，满清是从其分裂出的新兴强权。
- 东海是独立世界，不是若干一次性副本。

---

## 3. 任务层级

| 类型 | 时长 | 影响 |
|---|---:|---|
| 天下大主线 | 数十小时 | 改变国家、城池、战争与世界状态 |
| 区域大主线 | 5–15 小时 | 改变区域势力、城市与门派关系 |
| 人物小主线 | 1–5 小时 | 改变 NPC 阶段、关系与局部结果 |
| 支线 / 练级 | 5 分钟–1 小时 | 获得属性、关系、钱财、技能与线索 |

主线不会因玩家未接取而停止。世界时间推进人物命运、战争与区域状态。

---

## 4. 原著人物：命运事件模型

每位核心人物使用**分阶段状态**而非固定 NPC。乔峰示例：

```text
丐帮帮主 → 杏子林事件 → 调查身世 → 聚贤庄 → 雁门关 → 辽国南院大王
```

同一 NPC 不同阶段应有不同的称谓、地区、阵营、关系和任务。每个核心人物至少需要：

```text
姓名、所在区域、活动国家、出生政治背景、江湖阵营、朝廷关系、个人政治倾向、
江湖评价、朝廷影响力、江湖声望、危险程度、命运阶段、下一事件、世界时间条件
```

首批任务树连接关系见 `docs/gdd/03-launch-task-trees.md` 末尾"任务树连接"段。

---

## 5. 当前实现约定

### 5.1 UI 框架

- 左栏默认展示「江湖 / 行囊 / 武学 / 门派 / 游历」五项主导航，**中栏随导航切换**：江湖=当前区域任务与设施，游历=天下舆图（13 区卡片，可点击相邻区启程），武学/行囊=中栏大版面列表，门派=无门无派占位页。
- 江湖纸卷分页：第 1 页为本区历练（4 条任务 + 城中去处），其后每一页一棵原著任务树；「上一页 / 下一页」翻页，地名签可跳转。切区时回到历练页。无任务树的区域不显示翻页。
- 右栏页签：武学 / 行囊 / 传闻。属性三行（气血 / 内力 / 声望）显示当前值/成长上限；进度条使用固定高度 + `width: 100%`，**禁止子元素将角色栏撑成整列**。
- 右栏下方：查看详细属性 + 成就 / 信件 / 排行 / 设置四枚挂签，均为实弹窗功能（属性统计、派生成就、任务树来信、江湖排名、音效与重开设置）。
- 顶部：银两 / 修为徽记 + 管理员战斗测试倍率（×1/×10，仅放大自动交手的胜负历练与心得）+ **音乐 / 音效 / 配音** 三个独立开关 + 重开。旧存档 `mute` 会同时映射到三项。
- 无存档首访或重开后进入**开局创角**：名号输入、出身四选一（猎户/商贾/弃卒/书生）、天赋 5 点加点（根骨/臂力/悟性）、家传武学三选一；角色名与家传武学全站生效。
- 普通任务点击后先出**剧情卡**（场景 + NPC 台词 + 酬劳预览 + 动身/离开），再进入计时；台词来自 `CHATTER` 表（每区 4 条，与任务顺序一致）。
- 进度使用 `localStorage`（键 `jianghu-save-v1`）自动存档：游戏期间每 2.5 秒写入，关键行动状态变化立即写入，隐藏页签 / 关闭页时立即落盘（含尚未刷入界面的挂机历练）。
- 双端适配：`viewport-fit=cover`；`.app` 用 `env(safe-area-inset-*)` 避开刘海与系统状态栏。≤600px 顶栏三行（标题+重开，银两/修为，音乐/音效/配音/战斗倍率），徽记 22px；导航七格（江湖/行囊/武学/门派/游历/命运/**属性**）；「天下大势」长文隐藏。≤1100px 右栏角色卡不进文档流，点左栏「属性」浮层查看。设置面板也必须提供管理员战斗倍率开关。切片样式不得覆盖手机尺寸（尤其 `.currency i.em`）。
- **首屏性能**：UI 图全部为按显示尺寸压缩的 WebP（`public/art/`，合计约 0.5 MB）；禁止把 kit 源 PNG、未缩放切片打进 `public/`。字体用系统楷体/仿宋栈，**禁止** `@import` Google Fonts。全屏背景不得使用 `background-attachment: fixed`。剧情纸卷（`scroll-mission.webp`）仅在弹窗带 `.scroll` 时加载。挂机历练每 5 秒才写回 React 状态，SFX 复用 `Audio` 实例。
- 剧情正文按区域动态加载：首页只加载当前位置剧情，启程前加载目标区域，读档先恢复所需区域；`npm run content:index` 更新轻量索引，构建会检查索引是否过期。

### 5.2 任务树接入

- 当前兼容线性任务树（`trees` 数组，键 `{区}:{树ID}` 存进度）；新剧情目标为可改变命运的分支事件图，不能因旧数据格式强制回到原著结局。
- 节点以剧情弹窗呈现，遵循 `docs/gdd/07` 五模块框架：**情境描摹、NPC 台词、交互抉择、判定与回响、世界传闻**。
- 抉择支持能力检定（`diff`，不足走软失败仍推进）与消耗型选项（银两 / 物品）；回响支持 `voice` 配音字段。
- 整树完成发放打包奖励并解锁传闻、信件（`letters`）与秘藏物品。
- 已实装剧本（8 棵）：江南 YZ-01《市井小宝》、FZ-01《福威血夜》、JX-01《醉仙楼》、SZ-01《燕子坞》、HZ-01《梅庄四友》、WX-01《杏子林变局》；大理 DL-01《无量山风波》、DL-02《南帝旧踪》（详见 `docs/gdd/03` 与 `docs/gdd/quests/`）。
- 实现优先级（`docs/gdd/03` 末尾）：余下 DL-03 跨区线。

### 5.3 城市设施

按「同功能、异域皮肤」命名（`FAC` 表，未列区用通用名）：

| 设施 | 主要功能 |
|---|---|
| 客栈 | 打听消息（-2 银两解锁传闻）、投宿（-5 银两，气血/内力全满）、存档 |
| 药铺 | 金创药 20 / 解毒丸 15 |
| 武馆 | 切磋无死亡惩罚，胜 +15 / 负 +8 历练（**低等级升级保险**） |
| 镖局 | 跨城护镖循环（★ 短 / ★★ 中 / ★★★ 长） |

详细 17 项公共设施 + 城市模板规范见 `docs/gdd/06-map-infrastructure.md`。

### 5.4 音频

完整规范见 `docs/gdd/05-audio-design.md`。要点：

- BGM 13 区主题曲置于 `public/audio/bgm/zone/T01…T13-*.mp3`，按当前区域循环播放。
- 切区时旧曲 0.4 秒淡出、新曲 1.2 秒淡入（交叉淡化）；同曲恢复时补淡入（防无声 bug）。
- BGM 音量 0.5；浏览器自动播放限制下由首次点击解锁。
- 现有 SFX：`public/audio/{wood-pluck, quest-complete, breath-bell}.wav`——分别作为 UI 确认/受挫、任务完成、突破/抵达/疗伤的基础音色；音量默认最大（1.0）。
- NPC 对白位于 `public/audio/voice/<树ID>_<节点>_<角色>.mp3`（100 个全部接入）：剧情弹窗开节点后**按台词顺序连播**（句间约 0.4 秒气口），当前句高亮；♪ 从该句起重听后续；关闭/抉择时中断队列。对白期间 BGM 自动 duck 至 30%，队列结束恢复。
- 客栈「打听消息」播放地域语音轮换（`INN_VOICES`：江南 8 条 / 大理 3 条）。
- 顶栏与设置内三项独立开关：`muteBgm`（区主题曲）、`muteSfx`（点击/完成/突破）、`muteVoice`（对白连播与客栈打听）。关闭配音会中断当前队列；关闭音乐只暂停 BGM，不影响对白。
- BGM / SFX 生成提示词：`prompts/wuxia-bgm.txt`、`prompts/wuxia-sfx.txt`。

### 5.5 区域行走

- 区域之间按邻接关系（`LINKS`）行走，**只能前往相邻区域**，赶路需耗时并可能触发旅途随机事件。
- 各区以「凶险度」标签提示推荐能力；实力不足时支线有概率受挫（掉血、少量历练），小主线由剧情 NPC 协助，**禁止卡关**。
- 每区一次性任务链遵循：区域小主线 → 支线一 → 支线二 → 支线三；另有晨课练招、行脚差事、护送药车三类循环历练。任务需耗时完成，进行中不可再行动。

### 5.6 状态字段

| 字段 | 含义 |
|---|---|
| `name` / `attrAb` / `bonusSkill` | 角色名 / 出身与加点能力加成 / 家传武学 |
| `expTotal` | 总历练（等级 = `floor(expTotal/100)+1`，派生不再回写） |
| `hp` / `mp` / `silver` / `rep` | 当前气血比例 / 当前内力比例 / 银两 / 声望；实际上限随等级成长 |
| `favor` | NPC 好感（分段：陌生 / 点头 / 好感 / 莫逆 / 仇怨） |
| `rumors` | 已解锁传闻池；客栈打听从 `RUMORS` 解锁 |
| `letters` | 任务树完成来信（信件面板） |
| `visited` | 足迹：已抵达的大区索引 |
| `done` / `treeDone` | 各区任务链进度 / 任务树进度（键 `{区}:{树ID}`） |
| `items` | 行囊物品计数（`ITEMS` 表，`lore` 为信物/秘藏不可用） |
| `action` | 进行中的计时行动（travel / quest / routine / spar） |
| `devMult` | 管理员战斗测试倍率（1/10，仅自动交手） |
| `idle` / `loc` | 挂机开关 / 当前区域 |
| `training` / `idleBank` | 各武学与内功心得 / 待领取挂机盘缠 |
| `routineDone` | 各区域循环历练成功完成次数（键 `{区}:{类型}`） |
| `worldTime` / `fate` | 统一世界时间 / 当前命运节点、进入时间与历史 |
| `factionRelations` | 北、东、西丐帮之间的关系值 |
| `muteBgm` / `muteSfx` / `muteVoice` | 关音乐 / 关音效 / 关配音 |
| `flag` | 世界标记（任务树推进、阵营切换、NPC 阶段变更） |

---

## 6. 剧本与对话铁律

任何新写的剧情节点都必须遵循（详见 `docs/gdd/07-dialogue-framework.md`）：

1. **拒绝主角替身**：玩家是自己江湖经历的主角，可介入并改变原著人物命运，但不自动继承原著主角的身份与经历。
2. **客观白描与金古笔力**：禁止"叮 / 宿主 / 暴击伤害 9999"等网文词入对话。
3. **生活化与生态感**：所有任务从客栈传闻、告示牌、茶摊闲话、NPC 抱怨中自然引出。
4. **选择必有回响**：每个分支选项都需配齐情景反馈、软失败描述、长线江湖后果。
5. **四级对话分层**：境遇层（开场） → 动机层（陈述危机） → 交涉与检定层（属性 / 身份） → 余波与江湖传闻（结算回响）。

---

## 7. 高危 NPC 处理

- 遇见显著高于玩家能力的 NPC 时，**必须有系统危险提示**。
- 低等级玩家面对岳老三、云中鹤等人物应能**逃跑、受伤、取得线索或由剧情脱身**，不得强制击杀。
- 高等级玩家可击退高危人物并改变其核心命运事件；必须记录人物阶段、后继事件与跨区域后果。低等级玩家仍有逃跑、求援和软失败路径。
- 所有高危 NPC 必须有明确的境界标签和危险度字段（见 `docs/gdd/02` NPC 状态规范）。

---

## 8. 目录与资源约定

| 用途 | 位置 |
|---|---|
| 可读策划文档 | `docs/gdd/`（含 `quests/` 子目录） |
| UI 素材规范 | `docs/ui/` |
| AI 生成提示词（图像 / 音频） | `prompts/` |
| 应用代码 | `src/`（React + Vite） |
| 静态资源（站点用） | `public/`（`art/**/*.webp`、`audio/`） |
| UI / 场景图 PNG 源 | `art-src/`（不部署；`scripts/optimize-art.py` 写出 WebP） |
| 区主题曲 BGM | `public/audio/bgm/zone/T01…T13-<slug>.mp3` |
| 通用 SFX | `public/audio/*.wav` |
| UI 切片 WebP | `public/art/ui/slices/`（源 PNG 在 `art-src/ui/slices/`，脚本 `scripts/slice-ui.py` + `optimize-art.py`） |
| 脚本工具（音频生成、切图、压图、部署） | `scripts/`（`deploy.mjs` 为跨平台上线入口，`deploy.sh` 保留给 Linux） |
| 第三方依赖 | `node_modules/`（git 忽略） |
| 构建产物 | `dist/`（git 忽略） |
| 本地密钥 | `Key.txt`（git 忽略；脚本读取此文件） |

图像素材命名保留版本号（`-v1`），新版本用 `-v2` 等递增；**禁止覆盖已有素材**。

---

## 9. 注意事项与红线

- 神兵红线：倚天剑、屠龙刀、玄铁重剑、金蛇剑等天下至宝**严禁由铁匠铺打造**，必须通过专属原著史诗任务、秘境探索或机缘获取（`docs/gdd/06` 三）。
- 城市设施外观与文本必须按区域文化"异域皮肤"包装（`docs/gdd/06` 四），不可一个模板走天下。
- 任务文案五模块结构**不可省略任一**；纯 NPC 对话节点也需要 `dialogues` 与 `hearsay`。
- 所有音频资源须遵守 `docs/gdd/05` 音量基线与循环规范；不接受硬切循环。
- 任何把数值、按钮标签、剧情文案烘焙进图片的做法一律打回重做（见 `docs/ui/UI-ASSET-GUIDE.md` 第 5 条）。
- 打开卡顿优先查首屏图体积与字体，不要先怪服务器内存：本站是 nginx 静态资源，2G RAM 足够；瓶颈是 PNG 体积、Google 字体和每秒整页刷新。新图必须按显示尺寸出 WebP，源 PNG 只进 `art-src/`。

### 9.1 推荐优化

- 新增系统前先阅读 `docs/architecture-optimization.md`，优先把规则放入 `src/game/`，把内容放入 `src/content/`，组件只负责展示与派发动作。
- 任务树必须保持单一数据源；每棵树独立文件，构建时校验节点、奖励、物品、语音与前置引用。
- 玩家状态统一经 reducer/action 更新；挂机累计、剧情结算、音频播放等易变状态禁止使用模块级可变变量。
- 存档必须带版本号与迁移函数；任务进度应记录选择、世界 `flag` 与 NPC 阶段，而不只记录完成数量。
- 大型剧情按区域动态加载；使用 selector、`memo` 和稳定 props，避免挂机定时器导致整页刷新。
- 涉及状态、计时、随机结算或存档的改动，提交前至少运行 `npm run build`，并补对应回归测试。

### 9.2 架构警示

- `src/main.jsx` 是当前过渡期单体文件；不得继续向其中追加大型数据表、系统规则或新弹窗，新增代码应先落到目标目录。
- `idleHold`、音频实例、定时器等模块级变量若未在重开、读档、卸载时清理，可能造成角色串档、重复奖励或资源泄漏。
- `load()` 不得继续依赖浅合并兼容未来字段；任何 `GameState` 字段变更都必须更新迁移与默认值。
- 不得在核心结算中直接调用不可注入的 `Math.random()`，否则线上问题无法重放。
- `scripts/deploy.sh` 的线上替换必须可回滚；构建失败、内容校验失败或资源缺失时不得上线。
## 10. 部署

- 目标：阿里云 SSH 别名 `aliyun_ecs`（47.108.114.17，可用 `JIANGHU_DEPLOY_HOST` 覆盖），nginx 站点 `/etc/nginx/conf.d/jianghu.conf`，静态根目录 `/srv/jianghu`，端口 8082（域名 `wuxia.47.108.114.17.sslip.io:8082`）。
- 流程：开发完成 → `npm run deploy`（`scripts/deploy.mjs`：本地 test/build → scp 原子替换 → 直接生效，无需 reload nginx）。
- 每次上线以 deploy 脚本内的 build 为验证；构建失败不得上线。

### 10.1 自动交付约定

- 用户已授权：每次完成代码或项目文档改动后，自动构建、部署，部署后测试通过再提交 Git、推送远端，无需再次询问。部署脚本中的发布前测试继续保留。纯讨论不触发发布。
- 提交前完成必要验证；部署必须使用 `npm run deploy` 内的构建，任一步失败须停止后续发布并报告原因，禁止带失败结果上线。
- 仅提交本次任务相关文件，不提交密钥或无关改动。发布保留上一版本以便回滚；完成后报告提交、推送、构建与部署结果。

## 11. 最新玩法方向与实现

- 战斗采用战前武学配置、属性驱动的自动交手，以文字战报呈现招式、闪避、招架、伤势和调息，省略动画与逐回合手动选招。
- 已接入三类武馆对手、19 个剧情战斗入口（15 个主动交手、4 个失手追兵战）、8 项战斗历练和旅途山贼遭遇；调查与交涉仍使用非战斗检定。新打斗必须接入统一自动战斗，不得退回“一次能力判定 + 胜负文字”。规则在 `src/game/combat.js` / `world-engine.js`，内容在 `src/content/`，界面在 `src/features/combat/`。
- 八棵任务树已拆至 `src/content/quests/<ID>.js`，世界区域在 `src/content/world.js`；新增剧情只修改对应内容文件。明确高危高手的境界、危险度与交手目标，支持撑过指定回合、撤退和软失败继续。
- 存档格式升级为 `{ version: 5, savedAt, state }`，兼容 v1/v2/v3/v4 与旧键 `jianghu-save-v1`；支持备用恢复、三个手动槽位、导入导出、循环支线、行动、战斗、气血/内力及命运事件续存。重开仅清除自动存档及其备用，保留手动槽位。
- 新状态：`loadout`、`battle`、`rngState`、`questChoices`、`npcStates`、`flag`。交手按息推进，不计算离线战斗；战中每回合保存，读档不得重掷随机数或重复结算。
- 已实装首个动态命运事件图“三帮失粮案”：**北丐帮帮主乔峰、东丐帮帮主洪七公、西丐帮帮主史火龙**可因玩家查证、调停、偏袒或超时形成同盟、停争或失和；结果写入 NPC 状态、世界标记和势力关系。
- 详见 `docs/gdd/09-auto-combat-and-saves.md` 与 `docs/gdd/10-changing-fates.md`。

## 12. P0 成长与固定支线（2026-09-07）

- 当前新增成长领域使用 v6 存档，兼容 v1-v5；`commands.js` 为 UI 玩法状态入口，`p0-engine.js` 与 `side-events.js` 处理成长和固定支线。
- 临安 13 房间、武当 3 房间、6 项教学、8 类活动以及《一车救命药》事件图已接入。详情与阶段边界以 `docs/gdd/11-idle-rpg-roadmap.md` 为准。
- P0 支线使用固定五模块文案；经历和事实存档。P1 才增加记忆文档与 LLM，模型不得决定奖励、伤害或人物生死。
- 老角色保留旧日领悟，新角色武学必须通过家传、师承或机缘取得。页面隐藏后只补算活动，不离线推进战斗、旅行和命运期限。

## 13. 下一步框架重构计划（2026-09-07，待开发）

### 13.1 定位、范围与文档优先级

- 用户以爱好者身份编写供自己游玩的文字武侠游戏，目标为文字版《金庸群侠传》，未来覆盖 PC 和 APP。优先探索、人物关系、选择后果和成长闭环，排行、赛季、多人及留存数值不优先。
- 重点借鉴同类游戏的文字表达、文案组织、剧情结构及功能框架。核心方向是统一世界状态驱动的文字 RPG：生活、修炼、战斗、剧情共同改变一个世界。
- 本节记录分析和下一步方案，不代表已实装，也不授权仅因读取本节就启动全部重构。当前版本边界见第 12 节和路线图；后续按用户指令逐批开发。
- 本节细化 `docs/gdd/11-idle-rpg-roadmap.md`：将通用条件组合与固定条件对白的底座前移至 P0 补强；大规模剧情迁移、复杂机会窗口、记忆和 LLM 仍属 P1。实际开发时同步路线图，避免阶段描述分歧。
- 第 5 节、第 11 节中的计时和 v5 存档描述属于旧实现记录；当前为 v6。取消空等是本节待开发项，不能将旧行为或待开发项写成新功能已完成。

### 13.2 当前问题与已有基础

| 领域 | 当前基础 | 需要解决的问题 |
|---|---|---|
| 命令入口 | `src/game/commands.js` 统一 UI 玩法命令 | 旧规则仍分散，不能继续向 `main.jsx` 堆积系统逻辑 |
| 剧情进度 | 旧任务完成数组、八棵线性树、新支线事件图、三帮专用命运逻辑 | 多套进度和结算方式并存，跨任务连接需要逐处接线 |
| 世界状态 | `flag`、`npcStates`、`p0.facts`、`p0.npcs` 等 | 事实查询和人物状态不统一，容易出现对白与实际世界不一致 |
| 支线 | `side-events.js` 已有条件、成本、效果、战斗续接与经历 | 条件和效果种类有限，缺少通用组合、条件对白与调度 |
| 成长 | 生活资源、师承、基本功、装备、考核与恢复已形成样板闭环 | 更多剧情尚未读取这些成长结果，也未反向改变成长机会 |
| 时间 | 行动倒计时、活动累计、战斗回合、命运时间 | 现实等待与游戏时间用途交错，删等待可能影响收益与事件期限 |
| 记忆 | 最近 200 条经历和持久事实 | 有限日志不是长期记忆文档；尚无 LLM 对白 |
| 多端 | React/Vite、浏览器存档和音频 | 存储、音频、后台恢复需平台适配，核心规则应可独立测试 |

《一车救命药》已体现跨系统后果：交付后药材回收价 3 两变 5 两，开放华山剑法引荐教学，相关固定对白变化。下一轮应将此机制推广为契约，不是为每条任务另写引擎。

### 13.3 源码参考及使用方式

以下为已核查仓库、README、授权声明及部分核心源码的静态分析，没有编译运行这些外部项目。原版官方源码与爱好者复刻须区分；不能据此认定原版《金庸群侠传》官方源码公开，也没有足够依据将《我来自江湖》《活侠传》等商业游戏列为开源项目。

| 项目与仓库 | 技术与实际参考点 | 集成决策及授权边界 |
|---|---|---|
| [群侠传，启动！](https://github.com/jynew/jynew) | Unity/C#，Lua 和可视化事件，完整样例与 MOD；参考世界状态、地图交互、物品使用与剧情续接 | 阅读结构后自行实现；README 的 MIT 表述不能替代实际 LICENSE，后者限制未经授权使用代码和资源，第三方插件另有授权 |
| [金庸群侠传 C++ 复刻](https://github.com/scarsty/kys-cpp) | C++，Cifa 剧情脚本；交谈、检查物品、战斗、入队、修改场景等通用指令 | 提炼指令模型，不照搬数字参数或全局单例；LICENSE 为 BSD-3-Clause，README 另有限制金庸题材牟利的声明，直接复用需厘清 |
| [金庸群侠传 X Silverlight 版](https://github.com/hansquirrel/JyGameSilverlight) | 公开旧版 0.6.x，XML 剧情、条件、结果、地图事件、时间触发；不是后续 Unity 版本 | 优先参考内容组织；GPL 与 README 禁止游戏元素商用的声明须同时核对，不整体移植旧技术栈 |
| [inkjs](https://github.com/y-lohse/inkjs) | MIT，Ink 的 JavaScript 实现；条件对白、分支汇合、叙事状态 | P1 选一条长剧情试验编写和存档成本，证明收益后再采用；不能替代战斗、奖励等规则引擎 |
| [Evennia](https://github.com/evennia/evennia) | BSD-3-Clause，Python 文字多人世界框架；房间、人物、物品、命令和持久化 | 参考世界对象建模，当前不引入完整多人后端 |

源码阅读入口：

- `jynew/jynew`：`jyx2/Assets/Scripts/GameSave/GameRuntimeData.cs` 记录人物、队伍、物品、世界数据、键值事实和事件计数；`GameMaps/GameEventManager.cs` 连接进入地点、交互、使用物品与脚本；`LuaCore/LuaExecutor.cs` 展示异步事件执行；`jyx2/Assets/Mods/SAMPLE` 提供原创样例剧情。
- `scarsty/kys-cpp`：`src/Event.h` 提供通用事件接口，`src/Save.h` 展示角色、地图、物品等运行数据的组织。借鉴接口职责，存档仍使用自己的版本化结构。
- `hansquirrel/JyGameSilverlight`：`JyGameSilverlight/JyGame/GameData/Story.cs`、`MapEvents.cs`、`TimeTrigger.cs` 与 `Scripts/storys` 展示条件、行动、结果、一次性事件及时间触发的分离。
- 内容借鉴流程：选取小段代表性事件，整理情境、人物动机、选择条件、成本、结果、持续后果与文本节奏，再写成本项目内容。具体引用记录来源仓库、版本或提交、原文件、改写说明及授权范围。
- 个人自用不等于全部素材授权；代码、对白、剧情、美术、音频和金庸作品相关权利分别核对。当前优先参考设计并自行实现，未来公开分发前再核对实际纳入内容，不把整个 Unity 工程或付费插件直接搬入网页游戏。

### 13.4 目标架构与统一执行链

| 层次 | 职责 | 实施约束 |
|---|---|---|
| 内容层 | 房间、NPC、物品、武学、对白和事件定义 | 静态配置与运行状态分离，稳定 ID、内容版本和引用校验；沿用 `src/content/` |
| 世界状态层 | 人物位置、关系、事实、任务阶段、游戏时间 | 先统一查询接口，再迁移存储；每项事实有确定来源，避免新旧字段长期双写 |
| 规则层 | 条件检查、成本、奖励、解锁 | 统一条件表达式与效果执行器；校验后原子结算，不在 UI 发奖励 |
| 事件层 | 显示对白、接受选择、等待战斗、恢复后继 | 主线、支线、门派、旅途共用执行契约，内容不同不产生新引擎 |
| 模拟层 | 生活产出、修炼消耗、战斗和时间推进 | 独立确定性结算，通过结构化结果接回事件 |
| 平台层 | 存储、音频、窗口、后台恢复 | 浏览器、PC、APP 共用内容和规则，以接口替换环境依赖 |

统一执行链：玩家行动 → 检查条件 → 执行规则 → 更新世界 → 记录经历 → 显示回响与后续入口。

- 继续以 `commands.js` 为命令入口，规则沿用 `src/game/`，UI 沿用 `src/features/`；只在职责确有需要时拆模块，不为了目录形式重写全部代码。
- 事件需保留事件 ID、节点 ID、选项 ID、待结算上下文及终局状态。战斗完成、失败、撤退必须回到相同事件上下文，不能二次随机或重复发奖。
- 统一条件支持 `all/any/not` 与明确的比较操作；可读取武学、身份、资源、关系、位置和事实。未知条件与效果应校验报错，不能静默放行。
- 效果通过受控接口修改资源、事实、关系、教学与道路资格；扣款、推进和奖励要么整体成功，要么不改变状态。
- 游戏随机源、时钟可注入；核心引擎不依赖 React、DOM、浏览器计时器或全局音频实例。

### 13.5 文案、剧情与世界回响契约

保留五模块：情境描摹、NPC 对白、交互抉择、判定回响、世界传闻。不要把参考作品只当作对白素材库，重点学习人物动机、信息揭露、选择代价与后果组织。

| 内容 | 下一步契约 |
|---|---|
| 出场条件 | 地点、人物状态、身份、前置事实；P0 建立组合条件底座 |
| 固定对白变体 | 根据关系、情报和任务结果选固定文本，明确优先级与默认回退 |
| 选项条件 | 分开定义是否显示与是否可选；不可选时说明缺少条件 |
| 后继 | 成功、失败、撤退、拒绝分别定义；允许汇合，持久事实不被汇合抹除 |
| 持续后果 | 改变交易、教学、人物位置、关系、道路和其他事件入口 |
| 创作记录 | 来源、改写说明、内容版本；具体字段随实施确定，不预先强制引入复杂编辑器 |

示例：护药的交涉与战斗路线可汇合到交付，但保存“交涉解决”“负伤护送”等事实，药商、师傅及后续事件读取事实产生不同回应。分支价值以持续后果判断，不能只换一句结算文字。

第一条贯通体验：生活获取材料 → 制药或筹钱 → 交药、交涉或战斗 → 人物关系变化 → 教学机会 → 修炼成长 → 下一处探索。其中制药属于后续生活深度，目前不能标为已实现。

### 13.6 倒计时、世界时间与收益处理

- 已核查：`commands.js` 设置赶路 10 秒及旧任务、循环历练配置秒数，`world-engine.js` 到零触发遭遇、战斗或奖励。它们不是资源加载；区域剧情由 `loadZoneQuests` 在启程前加载。
- 下一步删除赶路、旧一次性任务和战斗前无交互的现实空等。内容就绪即抵达、显示事件或交手；真实加载跟随请求，失败可重试，不设置最低等待时间或假进度。
- 持续劳动归并生活活动按时产出；护送等事件型循环通过节点和战斗结算。不能删掉计时后让重复点击无限领奖。
- 修炼、采药、劳动时间对应实际产出和消耗，保留活动计时、上限和离线结算。自动战斗计时用于回合推进和阅读，可另评估加速或即时结算。
- 现实时间与游戏时间分别建模。即时旅行或任务可扣除配置的游戏内时长；实施前明确命运期限、事件先后与每次时间推进入口，避免冻结命运、跳过事件或双重推进。
- 旧存档中的未完成行动需迁移为恰好一次的续接，不能直接丢弃或自动重复奖励。此节尚未实装。

### 13.7 分阶段集成顺序

| 阶段 | 范围 | 交付门槛 |
|---|---|---|
| P0-A 统一规则 | 世界事实查询、组合条件、统一效果；先提供旧数据适配 | 新支线不再增加专用判定代码，旧存档可继续游玩 |
| P0-B 统一事件 | 固定条件对白、后继、战斗续接、一次性结算；迁移护药与武当考核 | 多解法、失败恢复、条件回响和读档续接全部可验证 |
| P0-C 时间与平台接口 | 清除空等、整理循环收益与世界时间；隔离存储、音频、生命周期 | 不刷奖、不破坏命运，规则可脱离 React 测试 |
| P1 剧情贯通 | 八棵原著树和三帮命运逐步迁移；复杂事件调度、跨任务回响、记忆与 LLM | 成长改变剧情解法，剧情改变成长机会；模型故障不阻断流程 |
| P2 多端与扩展 | PC/APP 适配、同伴小队、更多区域门派、生活深度、多周目与按需云存档 | 同一规则和内容跨端复用，扩展通过既有契约接入 |

首批只选择《一车救命药》、武当考核、一条具有代表性的旧任务进行迁移验证。覆盖生活资源、师承成长、战斗续接与旧内容兼容后，再批量迁移；不一次性重写全部剧情。

- 先记录现有行为与存档样例，再建立查询适配和新契约；逐个入口切换，确认没有消费者后删除旧路径。
- 存档结构变化时升级版本，处理新旧事实冲突、内容版本变化与进行中事件。不能只做字段浅合并，也不把所有旧剧情强行标为完成。
- 内容校验检查稳定 ID、条件和效果类型、引用、后继、不可达节点、失败出口及结局可达性；图可达不等于条件可满足，仍需实际流程测试。
- 每批完成后遵循第 10.1 节：测试与构建 → 部署 → 部署后测试 → 通过后提交并推送，报告实现边界与剩余工作。

### 13.8 P1 记忆、LLM 与叙事工具集成

- 后端可以接入 LLM API，用于丰富对白、传闻和回响；这提升叙事表现，不自动提升运行性能或玩法完整性，并会增加网络延迟和调用成本。
- 权威事实由规则引擎产生，记忆文档从结构化事实和经历生成，按角色、存档分支隔离，包含来源、版本与过时状态；最近 200 条日志不能替代长期摘要。
- 后端保管密钥，只传递当前 NPC 可知信息及允许表达的事实。模型输出限制为表现内容，不直接决定奖励、伤害、解锁、战斗结果或人物生死。
- 断网、超时、错误与不一致结果回退固定文案；缓存需绑定相关事实和内容版本，防止世界变化后复用过时回应。
- 首先选择单个事件对比固定版与增强版，验证一致性、延迟、缓存和费用，再扩大范围。LLM 不是 P0 完成条件。
- Ink/inkjs 仅在长剧情编写确有收益时试点；其叙事状态不能成为第二套权威世界状态，外部函数和标签须经统一规则接口执行。

### 13.9 多端方案

- 当前继续 React/Vite；PC 可比较 Tauri 与 Electron，APP 可评估 Capacitor。此为候选，不是已决定引入的依赖，不为封装提前重写 UI 或切换 Unity。
- 先抽取存储读写、音频播放、前后台切换和时间恢复接口，保留同一命令、内容与版本化存档模型；文件导入导出优先于账号和云同步。
- 后续评估安装包、离线内容缓存、自动更新、文件权限、音频策略和移动后台限制；各端恢复必须经过同一幂等结算入口，防止重复补算。
- 同伴与小队仍为玩法扩展优先项；平台封装按实际测试需要安排，云存档、排行和多人不作为个人单机体验的前置条件。

### 13.10 验收与现版本体验基线

框架验收：

1. 新增典型支线主要修改内容配置，无需增加专用引擎分支；不以强行零代码扩展作为所有新机制的要求。
2. 选择成本与效果原子结算，刷新、重复命令、战败、撤退、读档均不重复发奖；随机结果不会重掷。
3. 同一 NPC 根据实际经历显示不同固定对白，分支汇合仍保留事实；剧情结果改变至少一个实际玩法入口。
4. 生活 → 学艺 → 修炼 → 装备配招 → 战斗 → 恢复 → 后续机会可完整游玩，没有贫穷重伤死局。
5. 取消无交互等待后，循环收益、旅行遭遇、世界时间和命运期限仍正确；真实资源加载失败可恢复。
6. 旧存档、新角色、进行中活动和战斗有迁移与恢复覆盖，内容检查与真实路线测试同时通过。
7. 桌面与手机验证创角、移动、对白、活动、战报、存档和后台恢复；多端实施后补各平台同存档兼容验证。

当前线上测试入口为 `http://47.108.114.17:8082/`。体验新成长限制前先导出原存档，再新建角色；旧角色保留历史武学，不能完整代表新学艺路径。建议路线：

| 步骤 | 尝试内容 | 观察点 |
|---|---|---|
| 创角和移动 | 出身、天赋、家传武学，临安房间移动 | 当前地点、对白和下一行动是否清楚 |
| 生活和恢复 | 帮工、采药、读书、静养 | 银两、潜能、草药、学识及恢复是否有明确用途 |
| 学艺和交手 | 城南武馆教学、基本功和武学修炼、兵器配招、陪练 | 条件与上限是否可理解，配置和成长是否影响交手 |
| 护药支线 | 战斗、备药交涉、败退后补药或修箱、交付 | 分支可完成且有恢复；回收价和教学资格改变 |
| 武当成长 | 荆襄山门入门、洒扫、教学、修炼、考核 | 贡献和修炼能否连接晋级与提高上限 |
| 存档与离线 | 活动中关闭页面后返回，手动存读档比较支线 | 回归收益、持久事实、活动与战斗续接是否正确 |

当前只运行一项持续活动，开始旅行或战斗会停止旧活动，结束后需重新开始。当前没有 LLM 对白、长期记忆文档、同伴小队，也尚未取消倒计时；八棵原著剧情与三帮命运没有全部融入新成长框架。

优先收集“不知道下一步做什么”“等待无聊”“收益没有后续用途”“选择结果不符合预期”等体验问题；按钮可点击和测试通过不能代替玩法闭环与实际游玩评价。
